/**
 * Main eQuotes App.
 *
 * Render the quotes app.
 *
 * @since 1.0.0
 */

'use strict';

/* eslint-disable no-console, no-undef */
eQuotes.attach(
	'app',
	/**
	 * The main App Component.
	 *
	 * @since 1.0.0
	 */
	{
		init() {
			alert( '' );
		}
	}
);
