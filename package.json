{
  "name": "e-quotes",
  "version": "0.1.0",
  "description": "E-Quotes plugin",
  "license": "GPL3+",
  "author": "Darvin da Silveira",
  "scripts": {
    "start": "npm run build && npm run dev",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "await-spawn": "^4.0.2",
    "gulp": "^4.0.2"
  }
}
